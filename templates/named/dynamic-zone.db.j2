$ORIGIN .
$TTL 1800
{{ cloud_domain }} IN SOA ns1.{{ infra_domain }}. hostmaster.{{ infra_domain }}. (
                         2011112904 ; serial
                         60         ; refresh (1 minute)
                         15         ; retry (15 seconds)
                         1800       ; expire (30 minutes)
                         10         ; minimum (10 seconds)
                          )
                     NS ns1.{{ infra_domain }}.
$ORIGIN {{ cloud_domain }}.
$TTL 60 ; 1 minute
